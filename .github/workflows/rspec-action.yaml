name: Test
on: [ push, pull_request]
jobs: 
  rspec:
    name: CI
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    - name: Rspec
      run: 'bundle exec rspec'